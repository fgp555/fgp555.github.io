{
  "name": "frankgp-com",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio estático sin runtime dependencies — este package.json existe solo para scripts de mantenimiento locales (bump de versión, generar el PDF del CV). El sitio publicado no depende de nada de esto.",
  "scripts": {
    "build:html": "node scripts/bump-version.js",
    "build:cv": "node scripts/generate-cv-pdf.js",
    "linkedin:profile": "node scripts/linkedin/generate-profile.js",
    "linkedin:auth": "node scripts/linkedin/get-token.js",
    "linkedin:post": "node scripts/linkedin/post.js",
    "postinstall": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
